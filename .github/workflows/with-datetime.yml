# Auto-generated file, see workflow-templates/with-datetime.yml

on:
  push:
    paths:
      - '.github/workflows/macos.yml'
      - '.github/workflows/run-macos'
name: With DateTime::Format::ISO8601
jobs:
  build:
    runs-on: 'macos-latest'
    steps:
      - uses: actions/checkout@v6
      - name: Setup Perl environment
        uses: shogo82148/actions-setup-perl@v1
      - name: Test and build
        run: |
          cpanm DateTime::Format::ISO8601
          cpanm ExtUtils::MakeMaker Test::Pod Test::Pod::Coverage
          perl -Mparent -e0 || cpanm parent
          cpanm --installdeps .
          perl Makefile.PL
          make test TEST_VERBOSE=1
